node_modules
.next
.git
docs
backups
*.md
.env*
!.env.example
coverage
e2e

# prisma/ is intentionally NOT excluded — schema and migrations are required at build time
# (prisma generate runs during docker build; migrations applied at deploy time)
